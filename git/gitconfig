[user]
    name = Nikita Rudenko
    email = rudenkornk@gmail.com
[pull]
    rebase = true
[rebase]
    autoStash = true
[alias]
    amend = commit --amend
    br = branch -vv
    ck = checkout
    cm = commit
    cp = cherry-pick
    ft = fetch --all
    fp = format-patch
    ll = log --oneline
    llg = log --oneline --graph
    mb = merge-base
    rb = rebase
    rv = remote -v
    sg = diff --staged
    st = status
[init]
    defaultBranch = main
[credentials]
    helper = store
[core]
    editor = vim

